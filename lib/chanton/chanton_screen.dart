import 'package:flutter/material.dart';

class ChantonScreen extends StatelessWidget {
  const ChantonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Table(),
    );
  }
}
